clint ------------------------------ server
            syn ->
            <- syn, ack
            ack ->
            message ->
            <- message
            GET ->
            <- 200 OK

TLS 1.3 sends some keys ride away
1 less round trip for TLS 1.3